// Re-export new components for backward compatibility
export { WaterMonitoringDashboard as default } from '@/features/dashboard'
export { DatasetsPage } from '@/features/datasets'
export type { PredictionInputData, WaterInputData } from '@/shared/types'

